const express = require('express');
const pool = require('../modules/pool');
const router = express.Router();

/**
 * GET route for onboarding
 */
router.get('/', (req, res) => {

});

/**
 * POST route for onboarding
 */
router.post('/', (req, res) => {

});

module.exports = router;